# Documentation

Keep docs aligned with live runtime code (`src/llmwiki/cli.py`, `src/llmwiki/utils/config.py`, `src/llmwiki/db/store.py`).

## Canonical Docs
- `../README.md`: setup and quick start
- `index.md`: overview and runtime surface
- `cli.md`: command and option reference
- `architecture.md`: runtime components and data flow
- `memory.md`: persistence and state model
- `deployment.md`: unified vs split orchestration
- `quartz.md`: Quartz content wiring and commands

## Project Memory
- See `.agents/plans/` for implementation plans.
- See `.agents/learnings/` for reusable lessons and project memory.
