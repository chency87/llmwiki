# Wiki Schema

This vault uses a simple Markdown knowledge structure.

Conventions:
- Save entity pages under `entities/`
- Save summaries under `summaries/`
- Save mental models under `models/`
- Save question answers under `qa/`
- Keep pages interlinked with `[[Wiki Links]]` when relevant
